{{~ #*inline "page"}}
<div class="container all-movies">
    <nav class="top-nav">
        <img src="/static/img/bible.rs.svg" alt="BIBLE.RS" height="100" width="100" class="logo">
    </nav>
    <nav>
        <ol>
            {{~ #each movies as |movie|}}
            <li>
                <a href="/movie/{{title}}">{{title}}</a>
            </li>
            {{~ /each}}
        </ol>
    </nav>
</div>
{{~ /inline}}
{{~> base ~}}
