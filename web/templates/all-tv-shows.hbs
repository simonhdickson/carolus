{{~ #*inline "page"}}
<div class="container all-tv-shows">
    <nav class="top-nav">
        <img src="/static/img/carolus.svg" alt="Carolus" height="100" width="100" class="logo">
    </nav>
    <nav>
        <ol>
            {{~ #each tv_shows as |tv_show|}}
            <li>
                <a href="/tv/{{title}}">{{title}}</a>
            </li>
            {{~ /each}}
        </ol>
    </nav>
</div>
{{~ /inline}}
{{~> base ~}}
