{{~ #*inline "page"}}
<div class="container error">
    <nav class="top-nav">
        <a href="{{links.books.url}}">
            <img src="/static/img/carolus.svg" alt="Carolus" height="100" width="100" class="logo">
        </a>
    </nav>
    <p class="message">{{message}}</p>
</div>
{{/inline}}
{{~> base}}
